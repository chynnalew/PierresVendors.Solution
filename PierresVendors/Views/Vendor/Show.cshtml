@{
  Layout = "_Layout";
}

@using PierresVendors.Models;

<div class="container">
  <h1>@Model["vendor"].Name</h1>
  <p>@Model["vendor"].Description</p>
  <h4>Order Log:</h4>
  @if (@Model["vendor"].Orders.Count == 0)
  {
    <p>This vendor hasn't ordered yet</p>
  }
  <ul>
  @foreach (Order order in @Model["orders"])
  {
    <li><a href='/vendor/@Model["vendor"].Id/order/@order.Id'>@order.Date : @order.Title</a></li>
  }
  </ul>
  <p><a href='/vendor/@Model["vendor"].Id/order/new' class="btn btn-dark">Add a new Order</a></p>
  <p><a href='/vendor' class="btn btn-dark">Return to Vendor List</a></p>
  <p><a href='/' class="btn btn-dark">Return to Main Page</a></p>
  <br>
  <p>the following action is PERMENATE! Don't click unless you're sure!</p>
  <form action='/vendor/@Model["vendor"].Id' method="post">
    <button type="submit" class="btn btn-dark">Delete this Vendor</button>
  </form>
</div>
