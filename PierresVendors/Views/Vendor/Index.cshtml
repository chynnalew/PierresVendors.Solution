@{
  Layout = "_Layout";
}
@using PierresVendors.Models;

<div class="conatiner">
  <h1>Vendor List</h1>

  @if (@Model.Count == 0)
  {
    <p>You don't have any vendors yet</p>
  } 

  <ul>
  @foreach (Vendor vendor in Model)
  {
    <li><a href='/vendor/@vendor.Id'>@vendor.Name</a></li>
  }
  </ul>
  <a href="/vendor/new" class="btn btn-dark">Add a Vendor</a>
  <br>
  <p><a href='/' class="btn btn-dark">Back home</a></p>
</div>